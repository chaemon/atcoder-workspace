include atcoder/extra/header/chaemon_header

const DEBUG = true

proc solve(a:int, b:int, c:int) =
  echo 21 - a - b - c
  return

# input part {{{
block:
  var a = nextInt()
  var b = nextInt()
  var c = nextInt()
  solve(a, b, c)
#}}}

