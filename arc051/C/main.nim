include atcoder/extra/header/chaemon_header

const MOD = 1000000007

proc solve(N:int, A:int, B:int, a:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var A = nextInt()
  var B = nextInt()
  var a = newSeqWith(N, nextInt())
  solve(N, A, B, a)
#}}}
