include atcoder/extra/header/chaemon_header


proc solve(a:int, b:int, c:int, d:int) =
  echo a * d - b * c
  return

# input part {{{
block:
  var a = nextInt()
  var b = nextInt()
  var c = nextInt()
  var d = nextInt()
  solve(a, b, c, d)
#}}}
