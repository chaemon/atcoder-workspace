include atcoder/extra/header/chaemon_header


const DEBUG = true

proc solve(N:int, K:int, a:seq[int], t:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var K = nextInt()
  var a = newSeqWith(N, nextInt())
  var t = newSeqWith(K, nextInt())
  solve(N, K, a, t)
#}}}

