include atcoder/extra/header/chaemon_header


proc solve(N:int, A:int, B:int, h:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var A = nextInt()
  var B = nextInt()
  var h = newSeqWith(N, nextInt())
  solve(N, A, B, h)
#}}}
