include atcoder/extra/header/chaemon_header


const DEBUG = true

proc solve(N:int, A:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var A = newSeqWith(2*N-1-0+1, nextInt())
  solve(N, A)
#}}}

