const
  DO_CHECK = true
  DEBUG = true
  DO_TEST = false

include lib/header/chaemon_header


solveProc solve(N:int, t:seq[int], A:seq[int], B:seq[int]):
  return

when not DO_TEST:
  var N = nextInt()
  var t = newSeqWith(N, 0)
  var A = newSeqWith(N, 0)
  var B = newSeqWith(N, 0)
  for i in 0..<N:
    t[i] = nextInt()
    A[i] = nextInt()
    B[i] = nextInt()
  solve(N, t, A, B)
else:
  discard

