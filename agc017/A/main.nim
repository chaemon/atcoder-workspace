include atcoder/extra/header/chaemon_header

import atcoder/modint
const MOD = 2
type mint = modint2

proc solve(N:int, P:int, A:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var P = nextInt()
  var A = newSeqWith(N, nextInt())
  solve(N, P, A)
#}}}
