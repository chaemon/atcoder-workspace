include atcoder/extra/header/chaemon_header


proc solve(N:int, X:int, x:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var X = nextInt()
  var x = newSeqWith(N, nextInt())
  solve(N, X, x)
#}}}
