include atcoder/extra/header/chaemon_header


const DEBUG = true

proc solve(A:int, B:int, C:int) =
  echo [(A, 'A'), (B, 'B'), (C, 'C')].sorted[1][1]
  return

# input part {{{
block:
  var A = nextInt()
  var B = nextInt()
  var C = nextInt()
  solve(A, B, C)
#}}}

