include atcoder/extra/header/chaemon_header


const DEBUG = true

proc solve(N:int, X:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var X = newSeqWith(N-0+1, nextInt())
  solve(N, X)
#}}}

