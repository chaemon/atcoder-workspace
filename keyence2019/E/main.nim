include atcoder/extra/header/chaemon_header

import atcoder/dsu

proc solve(N:int, D:int, A:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var D = nextInt()
  var A = newSeqWith(N, nextInt())
  solve(N, D, A)
#}}}
