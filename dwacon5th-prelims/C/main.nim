include atcoder/extra/header/chaemon_header


proc solve(N:int, S:string, Q:int, k:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var S = nextString()
  var Q = nextInt()
  var k = newSeqWith(Q-1-0+1, nextInt())
  solve(N, S, Q, k)
#}}}
