include atcoder/extra/header/chaemon_header

import atcoder/modint
const MOD = 8
type mint = modint8

proc solve(N:int, P:seq[int]) =
  return

# input part {{{
block:
  var N = nextInt()
  var P = newSeqWith(N-1-0+1, nextInt())
  solve(N, P)
#}}}
