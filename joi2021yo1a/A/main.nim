include atcoder/extra/header/chaemon_header


proc solve(A:int, B:int, C:int) =
  echo [A, B, C].sorted[1]
  return

# input part {{{
block:
  var A = nextInt()
  var B = nextInt()
  var C = nextInt()
  solve(A, B, C)
#}}}
