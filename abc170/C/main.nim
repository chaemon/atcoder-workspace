include atcoder/extra/header/chaemon_header


proc solve(X:int, N:int, p:seq[int]) =
  return

# input part {{{
block:
  var X = nextInt()
  var N = nextInt()
  var p = newSeqWith(N, nextInt())
  solve(X, N, p)
#}}}
