include atcoder/extra/header/chaemon_header


const DEBUG = true

proc solve(X:int, Y:int, Z:int) =
  for T in countdown(10000000, 0):
    if Y * Z > T * X:
      echo T;return
  return

# input part {{{
block:
  var X = nextInt()
  var Y = nextInt()
  var Z = nextInt()
  solve(X, Y, Z)
#}}}

